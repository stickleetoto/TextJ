[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "textj"
version = "0.1.0.dev0"
description = "Ultra-fast local OCR utility for extracting text from images and screenshots."
readme = "README.md"
requires-python = ">=3.10"
license = { file = "LICENSE" }
authors = [
  { name = "stickleetoto" }
]
keywords = ["ocr", "korean", "screenshot", "onnxruntime", "rapidocr"]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "Environment :: Console",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Multimedia :: Graphics :: Capture :: Screen Capture",
  "Topic :: Scientific/Engineering :: Image Recognition",
]

dependencies = [
  "rapidocr>=3.9,<4",
  "onnxruntime>=1.20,<2",
  "psutil>=6,<8",
  "numpy>=1.26,<3",
  "Pillow>=10,<13",
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<10",
]

[project.scripts]
textj = "textj.app.cli:main"
textj-bench = "textj.app.bench:main"
textj-bench-suite = "textj.app.bench_suite:main"
textj-bench-compare = "textj.app.bench_compare:main"
textj-bench-runtime = "textj.app.bench_runtime:main"
textj-clipboard = "textj.app.clipboard:main"
textj-serve = "textj.app.serve:main"
textj-client = "textj.app.client:main"
textj-mcp = "textj.adapters.mcp_server:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
